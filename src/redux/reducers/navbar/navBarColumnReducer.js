
let initialState = {
    profile:'profile',
    news:'news',
    messages:'messages',
    friends:'friends'
}


export const navBarColumnReducer = (state = initialState, action) => {

    switch (action.type) {

        default:
            return state;
    }
}